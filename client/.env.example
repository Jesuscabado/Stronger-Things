# URL base del backend (sin barra final)
VITE_API_URL=http://localhost:3001

# Client ID de Google OAuth (opcional — solo si activas el login con Google)
# Obtén el valor en https://console.cloud.google.com → APIs → Credenciales
VITE_GOOGLE_CLIENT_ID=
